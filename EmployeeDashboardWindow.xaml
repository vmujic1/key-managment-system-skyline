<Window x:Class="key_managment_system.EmployeeDashboardWindow"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:key_managment_system"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="EmployeeDashboardWindow">
    <Window.Resources>
        <ResourceDictionary>
            <Style x:Key="btnS" TargetType="{x:Type Button}">
                <Setter Property="Height" Value="30"/>
                <Setter Property="Width" Value="100"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Margin" Value="5,0"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>
    
        <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
        </Grid.RowDefinitions>
            
        <Grid Background="Navy">
            <StackPanel Background="Navy" Orientation="Horizontal" HorizontalAlignment="Center">
                
                <Button Style="{StaticResource btnS}" Content="Requests"/>
                <Button Style="{StaticResource btnS}" Content="Card using"/>
                <Button Style="{StaticResource btnS}" Content="Log Out"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
